services:
  frontend:
    build:
      context: .
      target: runner
    container_name: ro_manic_frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
